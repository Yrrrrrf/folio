#import "namespaces/metadata.typ": metadata-defaults
#import "namespaces/initiation.typ": initiation-defaults
#import "namespaces/baselines.typ": baselines-defaults
#import "namespaces/registers.typ": registers-defaults
#import "namespaces/governance.typ": governance-defaults
#import "namespaces/closure.typ": closure-defaults

#let default-data = (
  metadata: metadata-defaults,
  initiation: initiation-defaults,
  baselines: baselines-defaults,
  registers: registers-defaults,
  governance: governance-defaults,
  closure: closure-defaults,
)

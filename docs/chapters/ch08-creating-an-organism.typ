// intentionally does not need the fixture
= Creating an Organism

Because Folio separates data from presentation, creating new documents (organisms) is fully decoupled from the core structure. You can use `#import "src/kinds/_manifest.typ": compose` and supply your dictionary and required paths to build robust templates.

#import "_fixture.typ": project
= Metadata

The `metadata` namespace is a cross-cutting pillar. It conditions every rendered page. 

It tracks core attributes like the project `name`, `client_name`, `version`, and `confidentiality`. This data guarantees auditability and standardizes branding across headers and footers.
